<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="org.brainail.everboxing.lingo"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name="org.brainail.everboxing.lingo.app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lingo"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">

        <activity
            android:name="org.brainail.everboxing.lingo.ui.home.LingoHomeActivity"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Lingo.NoActionBar"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts"/>
    </application>
</manifest>